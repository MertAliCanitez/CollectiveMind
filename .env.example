# ─────────────────────────────────────────────
# Database
# ─────────────────────────────────────────────
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/collectivemind_dev"
TEST_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/collectivemind_test"

# ─────────────────────────────────────────────
# Clerk Authentication
# ─────────────────────────────────────────────
# Get these from https://dashboard.clerk.com
CLERK_SECRET_KEY="sk_test_REPLACE_ME"
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_REPLACE_ME"

# Clerk webhook secret (from Clerk dashboard → Webhooks)
CLERK_WEBHOOK_SECRET="whsec_REPLACE_ME"

# Clerk redirect URLs — must match the local app routes exactly.
# NEXT_PUBLIC_CLERK_SIGN_IN_URL is critical: without it, auth.protect() in
# middleware falls back to the Clerk-hosted sign-in page (accounts.dev domain)
# instead of the branded local /sign-in page, creating two different sign-in flows.
NEXT_PUBLIC_CLERK_SIGN_IN_URL="/sign-in"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/sign-up"
# Post-auth redirect (non-deprecated Clerk v6 names):
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL="/home"
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL="/onboarding"

# ─────────────────────────────────────────────
# App URLs
# ─────────────────────────────────────────────
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_DASHBOARD_URL="http://localhost:3001"
NEXT_PUBLIC_ADMIN_URL="http://localhost:3002"

# ─────────────────────────────────────────────
# Billing / Payment (deferred — leave as null for v1)
# ─────────────────────────────────────────────
# PAYMENT_PROVIDER="stripe"
# STRIPE_SECRET_KEY="sk_test_REPLACE_ME"
# STRIPE_WEBHOOK_SECRET="whsec_REPLACE_ME"
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_REPLACE_ME"

# ─────────────────────────────────────────────
# Observability (optional — structured logs are always on)
# Set SENTRY_DSN to enable error tracking in production.
# ─────────────────────────────────────────────
# SENTRY_DSN="https://REPLACE_ME@o0.ingest.sentry.io/0"
# NEXT_PUBLIC_SENTRY_DSN="https://REPLACE_ME@o0.ingest.sentry.io/0"
# SENTRY_ENVIRONMENT="production"  # staging | production
# SENTRY_TRACES_SAMPLE_RATE="0.1"  # 10% of transactions
